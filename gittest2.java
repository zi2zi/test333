import java.util.HashMap;
import java.util.Map;
public class Cache {
      
       public static void main(String[] args) {            
              try(GZip gzip = GZipInputStream.create(url.openStream()))
              {
                  use(gzip);
              }
              catch(Exception e){
                handle();
              }
       }
}
